const name = 'irvin' -> This will be constant through out code. cannot change.
const person = { name: 'seb', job: 'coder'}
Object.entries(person)

[["name","seb"], ["job", "coder"]]

var myMap = new Map(Object.entries(person))
